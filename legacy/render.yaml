services:
  - type: web
    name: poster-pro
    env: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: REPLICATE_API_TOKEN
        sync: false
      - key: NODE_ENV
        value: production
    healthCheckPath: /health

